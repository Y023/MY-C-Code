#include <stdio.h>
struct Book
{
	char name[14];
	short price; 
};
int main()
{
	struct Book b1={"C语言程序设计",45};
	struct Book*pb=&b1;
	printf("%s",(*pb).name);
	printf("%d",(*pb).price);  
	return 0;
}
